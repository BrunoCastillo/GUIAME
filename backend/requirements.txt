# Dependencias mínimas para Vercel
# Versión ultra-optimizada para evitar errores de memoria

# Core FastAPI (mínimo)
fastapi==0.104.1
mangum==0.17.0

# Database (mínimo necesario)
sqlalchemy==2.0.23
psycopg2-binary==2.9.9

# Configuración
python-dotenv==1.0.0
pydantic==2.5.0
pydantic-settings==2.1.0

# Seguridad básica
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.6

# Utilidades mínimas
httpx>=0.24.0,<0.25.0
email-validator==2.1.0

# Supabase
supabase>=2.0.3

# NOTA: Dependencias removidas para reducir memoria:
# - uvicorn (no necesario en serverless)
# - alembic (ejecutar migraciones manualmente)
# - pgvector (instalar extensión en Supabase directamente)
# - aiofiles (no crítico)
# - slowapi (rate limiting opcional)
# - google-auth (solo si se usa)
# - PyPDF2, python-docx, etc. (procesamiento pesado)
# - redis, celery (tareas asíncronas opcionales)
